#include "main.h"
/**
 * _isupper - function name
 * @c: variable
 * Return: Always (0) Success
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}

