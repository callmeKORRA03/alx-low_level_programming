#include <stdio.h>
/**
* main - main function
* @argc: not used in this program
* @argv: argument vector used to print out the file name
* Return: Always (0)
*/
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
